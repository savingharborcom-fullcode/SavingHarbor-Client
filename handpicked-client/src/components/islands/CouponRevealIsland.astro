---
import CouponReveal from "../couponReveal.jsx";

const { coupon, storeSlug, affiliateUrl } = Astro.props;
---
<CouponReveal client:visible coupon={coupon} storeSlug={storeSlug} affiliateUrl={affiliateUrl}/>
